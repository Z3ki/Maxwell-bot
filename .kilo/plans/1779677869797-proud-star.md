# Plan: Fix HTML unescaping in Tool bodies

The Lua script was originally generated by the AI but was incorrectly modified by the bot's framework. The framework (in `/root/maxwell/bot.py`) unescapes HTML entities in tool parameter bodies, meaning `&amp;` becomes `&` and `&lt;` becomes `<` when the AI outputs the `<content>` block.

Here is what needs to be fixed:

1. **Modify `_parse_tool_body_params`**:
   - Instead of calling `html.unescape` unconditionally, skip it for specific fields that usually contain literal code blocks, like `content`, `code`, `body`, and `command`.
   - Update the default fallback `cleaned_body` unescaping to also respect the `default_param` name and skip unescaping if it's one of these fields.

2. **Test the changes locally**:
   - Verify that `<content>` parameters keep their original HTML entities intact.
   - Verify that other parameters (e.g. `message`) still have HTML entities correctly unescaped.

## Execution Steps
1. In `/root/maxwell/bot.py`, update `_parse_tool_body_params` to use selective unescaping.
2. In `/root/maxwell/bot.py`, verify `_parse_loose_tool_params` and `_parse_xml_attrs` - they generally handle quoted strings, so `html.unescape` is fine there, but if needed, we can also disable it for code parameters (though typically these use the body parameter).
3. Validate and confirm.